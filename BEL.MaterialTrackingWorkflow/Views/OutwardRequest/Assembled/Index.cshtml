@using BEL.MaterialTrackingWorkflow.Models.OutwardRequest
@using BEL.CommonDataContract
@using BEL.MaterialTrackingWorkflow.Models.Common
@model  OutwardAssmblContract
@{
    OutwardAssmblContract contract = Model as OutwardAssmblContract;
    OutwardAssmblRequestForm form = contract.Forms.FirstOrDefault() as OutwardAssmblRequestForm;
    ViewBag.Title = "Outward Assembled Request";
    TesterSection testerSection = form.SectionsList.FirstOrDefault(f => f.SectionName.Equals(OutwardSectionName.TESTERSECTION)) as TesterSection;
    HODSection hodSection = form.SectionsList.FirstOrDefault(f => f.SectionName.Equals(OutwardSectionName.HODSECTION)) as HODSection;
    RecipientSection recipientSection = form.SectionsList.FirstOrDefault(f => f.SectionName.Equals(OutwardSectionName.RECIPIENTSECTION)) as RecipientSection;
}

@if (form != null)
{
    @section buttons{
        @Html.Partial("_ButtonGroup", form.Buttons)
    }

    if (!string.IsNullOrEmpty(form.FormStatus))
    {
        <div class="card-body text-right pad-bot-15">
            <div class="label label-primary">
                @form.FormStatus
            </div>
        </div>
    }


    <div id="divOutwardAssForm">
        <div id="tab-general">
            <div class="row mbl">
                <div class="col-lg-12">
                    <div class="tab-content responsive " id="generalTabContent">

                        @if (testerSection != null)
                        {
                            string sectionActiveCss = testerSection.IsActive ? string.Empty : "disabled";
                            //if (form.Buttons.Any(p => p.Name == "Release" && testerSection.Status == "Sent Back" && (hodSection != null && hodSection.Action == "Approve")))
                            //{
                            //    sectionActiveCss = "disabled";
                            //}
                            <div class="card @sectionActiveCss">
                                <div class="card-head" data-toggle="collapse" data-target="#sectionAssTester">
                                    @Html.ResourceValue("Section_Title_TESTER", ResourceNames.OutwardRequest)
                                    <div class="btn collapse-btn"><i class="fa fa-chevron-down"></i></div>
                                </div>
                                <div class="card-body" id="sectionAssTester">
                                    <div class="form-horizontal">
                                        <p><span class="text-danger text-default-light">@Html.ResourceValue("Text_Mandatory", ResourceNames.Common)</span></p>
                                        @Html.Partial("../OutwardRequest/Assembled/_assembleTesterForm", testerSection)

                                    </div>
                                    <div class="clearfix"></div>
                                </div>
                                <div class="clearfix"></div>
                            </div>
                        }
                        @if (hodSection != null)
                        {
                            string sectionActiveCss = hodSection.IsActive ? string.Empty : "disabled";
                            <div class="card @sectionActiveCss">
                                <div class="card-head" data-toggle="collapse" data-target="#sectionAssHOD">
                                    @Html.ResourceValue("Section_Title_HOD", ResourceNames.OutwardRequest)

                                    <div class="btn collapse-btn"><i class="fa fa-chevron-down"></i></div>
                                </div>
                                <div class="card-body" id="sectionAssHOD">
                                    <div class="form-horizontal">

                                        @Html.Partial("../OutwardRequest/Assembled/_assembleHODForm", hodSection)

                                    </div>
                                    <div class="clearfix"></div>
                                </div>
                                <div class="clearfix"></div>
                            </div>
                        }

                        @if (recipientSection != null)
                        {
                            string sectionActiveCss = recipientSection.IsActive ? string.Empty : "disabled";
                            <div class="card @sectionActiveCss">
                                <div class="card-head" data-toggle="collapse" data-target="#sectionAssRecipient">
                                    @Html.ResourceValue("Section_Title_RECIPIENT", ResourceNames.OutwardRequest)

                                    <div class="btn collapse-btn"><i class="fa fa-chevron-down"></i></div>
                                </div>
                                <div class="card-body" id="sectionAssRecipient">
                                    <div class="form-horizontal">

                                        @Html.Partial("../OutwardRequest/Assembled/_assembleRecipientForm", recipientSection)

                                    </div>
                                    <div class="clearfix"></div>
                                </div>
                                <div class="clearfix"></div>
                            </div>
                        }

                        @{ApplicationStatusSection appStatus = form.SectionsList.FirstOrDefault(f => f != null && f.SectionName.Equals(SectionNameConstant.APPLICATIONSTATUS)) as ApplicationStatusSection;

                        if (appStatus != null)
                        {
                            @Html.Partial("_ApplicationStatus", appStatus)
                        }

                        ActivityLogSection activityLog = form.SectionsList.FirstOrDefault(f => f.SectionName.Equals(INWARDSectionName.ACTIVITYLOG)) as ActivityLogSection;
                        if (activityLog != null)
                        {
                            @Html.Partial("_ActivityLogs", activityLog, new ViewDataDictionary { { "Resources", ResourceNames.OutwardRequest } })
                        }
                        }
                    </div>
                </div>
            </div>
        </div>

        <div class="scroll-top-wrapper ">
            <i class="fa fa-chevron-up">
            </i>
        </div>


    </div>

    if (testerSection != null)
    {
        if (testerSection.Status == "Completed" || testerSection.Status == "Rejected")
        {

            @Html.Partial("../OutwardRequest/Assembled/_printAssembleForm", Model)
        }
        else if (testerSection.Status == "Sent Back")
        {
            @Html.Partial("../OutwardRequest/Assembled/_printAssembleForm", Model)
        }
    }

}
@section scripts{
    @Scripts.Render("~/bundles/OutwardRequestindex")
}
